import React from 'react';


const style={
    padding:'10px',
    width:'100%'
}

const TextArea = (props) => (
    <div style={style}>
        <input type="textarea" />
    </div>
);

export default TextArea;