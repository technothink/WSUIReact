import React from 'react';


const style={
    padding:'10px',
    width:'100%'
}

const TextArea = (props) => (
    <div >
        <input type="textarea" style={style} />
    </div>
);

export default TextArea;